# Giotto Sources

Object defining a data backend for a Giotto object. The `gsource` class
is VIRTUAL. Specific backend types should extend from this class.

## Slots

- `path`:

  character. Filepath to use
