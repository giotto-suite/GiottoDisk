% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-parquetExprStore.R
\name{cellbin_gef_to_parquetExprStore}
\alias{cellbin_gef_to_parquetExprStore}
\title{Stream-convert a Stereo-seq cellbin .gef file into a parquetExprStore}
\usage{
cellbin_gef_to_parquetExprStore(
  gef_path,
  output_path,
  gene_column = c("geneName", "geneID"),
  batch_genes = 500L,
  overwrite = FALSE
)
}
\arguments{
\item{gef_path}{character. Path to a Stereo-seq cellbin .gef file.}

\item{output_path}{character. Destination Parquet file (single file
when total nnz <= \code{batch_genes} worth) or directory (one shard per
gene-chunk, used otherwise).}

\item{gene_column}{character. Which gene-id column to use as \code{feat_ids}:
\code{"geneName"} (default) or \code{"geneID"}.}

\item{batch_genes}{integer. Number of genes per streaming chunk.
Default 500.}

\item{overwrite}{logical. If \code{TRUE}, replace \code{output_path} if it exists.}
}
\value{
A \linkS4class{parquetExprStore} with \code{cell_ids = paste0("cell_", cellDT$id)} and \code{feat_ids} from the chosen \code{gene_column}.
}
\description{
Reads a Stereo-seq cell-bin GEF (\verb{*.cellbin.gef} / \verb{*.adjusted.cellbin.gef})
in \strong{gene-chunks} via \code{rhdf5::h5read} hyperslab and writes one Parquet
shard per chunk. \strong{No full triplet table is ever materialized in memory} —
only \code{batch_genes} genes' worth of \verb{(cellID, count)} rows are held at a time.

GEF cellbin layout (from BGI's SAW pipeline):

\if{html}{\out{<div class="sourceCode">}}\preformatted{cellBin/cell     : (id, x, y, ...) — small, n_cells rows
cellBin/gene     : (geneName, geneID, cellCount, offset, ...) — small,
                   n_genes rows. cellCount = nnz per gene; rows in geneExp
                   are stored gene-major (run-length encoded by cellCount).
cellBin/geneExp  : (cellID, count) — large, sum(cellCount) total rows.
}\if{html}{\out{</div>}}

Note on predicate pushdown: shards are sorted by \code{row_id} (= cell index)
within themselves, but each gene-chunk shard contains rows from many
cells. Chunk-level row_id stats are wide; downstream cell-range filters
touch every shard but read only matching rows within them.
}
\seealso{
\code{\link[=bin_gef_to_parquetExprStore]{bin_gef_to_parquetExprStore()}},
\code{\link[=h5_to_parquetExprStore]{h5_to_parquetExprStore()}}
}
