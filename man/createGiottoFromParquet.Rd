% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createGiottoFromParquet.R
\name{createGiottoFromParquet}
\alias{createGiottoFromParquet}
\title{Create a Giotto object backed by a parquet expression matrix}
\usage{
createGiottoFromParquet(
  parquet_path = NULL,
  mtx_dir = NULL,
  output_path = NULL,
  cell_ids = NULL,
  feat_ids = NULL,
  spatial_locs = NULL,
  chunk_size = NULL,
  feature_id_col = 2L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{parquet_path}{character. Path to an existing sorted long-format
parquet file or directory. Mutually exclusive with \code{mtx_dir}.}

\item{mtx_dir}{character. Directory containing \code{matrix.mtx.gz},
\code{barcodes.tsv.gz}, \code{features.tsv.gz} (10x / Xenium layout).
Mutually exclusive with \code{parquet_path}.}

\item{output_path}{character. Required when \code{mtx_dir} is supplied:
destination path for the parquet file or directory produced by
\code{\link[=mtx_to_parquetExprStore]{mtx_to_parquetExprStore()}}.}

\item{cell_ids}{character. Cell barcodes, length = n_cells. Required
when \code{parquet_path} is supplied; ignored (read from \code{barcodes.tsv.gz})
when \code{mtx_dir} is supplied.}

\item{feat_ids}{character. Gene / feature IDs, length = n_genes.
Required when \code{parquet_path} is supplied; ignored (read from
\code{features.tsv.gz}) when \code{mtx_dir} is supplied.}

\item{spatial_locs}{spatial locations passed through to
\code{\link[Giotto:reexports]{Giotto::createGiottoObject()}}.}

\item{chunk_size}{numeric. Streaming chunk size (cells per Arrow
read). \code{NULL} (default) means call \code{\link[=sc_recommend_chunk]{sc_recommend_chunk()}} using
currently free RAM and print a guidance line. Pass an integer to
skip the recommendation.}

\item{feature_id_col}{integer. Which column of \code{features.tsv} to use
as the gene identifier when \code{mtx_dir} is supplied. Default \code{2L}
(gene symbol).}

\item{verbose}{logical. Print progress messages. Default \code{TRUE}.}

\item{...}{additional arguments passed through to
\code{\link[Giotto:reexports]{Giotto::createGiottoObject()}} (e.g. \code{spat_unit}, \code{feat_type},
\code{instructions}).}
}
\value{
A \code{giotto} object whose \verb{@exprMat} slot in the "raw"
\code{exprObj} is a \linkS4class{parquetExprStore}.
}
\description{
Convenience wrapper that constructs a \code{giotto} object whose expression
backend is a \linkS4class{parquetExprStore}. Accepts either an existing sorted
long-format Parquet file (or directory of chunks), or a 10x / Xenium
MatrixMarket triple — in the latter case the streaming converter
\code{\link[=mtx_to_parquetExprStore]{mtx_to_parquetExprStore()}} is called and \strong{no \code{dgCMatrix} is ever
materialized}.

Internally:
\enumerate{
\item Builds the \linkS4class{parquetExprStore} (via direct construction or via
\code{\link[=mtx_to_parquetExprStore]{mtx_to_parquetExprStore()}}).
\item Wraps it in an \code{exprObj} via \code{new("exprObj", ...)} to bypass
GiottoClass's \code{.evaluate_expr_matrix} validator (which doesn't
recognize \code{parquetExprStore}).
\item Creates a \code{giotto} skeleton with a 1×1 stub matrix shaped like the
parquet store, then \code{setExpression(g, eo)} swaps the parquet store
in.
\item Optionally calls \code{\link[=sc_recommend_chunk]{sc_recommend_chunk()}} to print a chunk-size
recommendation based on currently free RAM.
}
}
\section{Two ways to call}{

\strong{From an existing parquet} — pass \code{parquet_path}, \code{cell_ids},
\code{feat_ids}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{g <- createGiottoFromParquet(
    parquet_path = "counts.parquet",
    cell_ids     = barcode_vec,
    feat_ids     = gene_vec,
    spatial_locs = locs_df
)
}\if{html}{\out{</div>}}

\strong{From a 10x MatrixMarket triple} — pass \code{mtx_dir}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{g <- createGiottoFromParquet(
    mtx_dir       = "Xenium_output/cell_feature_matrix/",
    output_path   = "Xenium_output/expression.parquet",
    spatial_locs  = locs_df
)
}\if{html}{\out{</div>}}

Streams \code{matrix.mtx.gz} to a sorted long-format Parquet, reads
barcodes/features from sidecars, then proceeds as above.
}

\seealso{
\code{\link[=parquetExprStore]{parquetExprStore()}}, \code{\link[=mtx_to_parquetExprStore]{mtx_to_parquetExprStore()}},
\code{\link[=sc_recommend_chunk]{sc_recommend_chunk()}}.
}
