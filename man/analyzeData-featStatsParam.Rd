% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-analyzeData.R
\name{analyzeData-featStatsParam}
\alias{analyzeData-featStatsParam}
\title{Streaming per-feature statistics}
\usage{
\S4method{analyzeData}{parquetExprBase,featStatsParam}(x, param, ..., groups = NULL, stats = NULL)
}
\arguments{
\item{x}{a \code{parquetExprBase} store.}

\item{param}{a \link[Giotto:featStatsParam-class]{Giotto::featStatsParam}.}

\item{...}{additional arguments (none used).}

\item{groups}{optional vector of group assignments, one per cell of the
current view, \code{NA} to exclude a cell. When supplied, the statistics are
taken per (feature, group) instead of over every cell, and the result gains
\code{group} and \code{n_cells} columns.}

\item{stats}{optional character vector of accumulators to compute, any of
\code{"sum"}, \code{"sumsq"}, \code{"nnz"}, \code{"sum_det"}. Grouped path only. Emitted
columns are whichever the requested accumulators support, so asking for
less genuinely scans for less. Defaults to all four.}
}
\value{
A \code{data.table}, one row per feature, or per (feature, group) when
\code{groups} is supplied.
}
\description{
\code{\link[GiottoClass:analyzeData]{GiottoClass::analyzeData()}} method for a \link[Giotto:featStatsParam-class]{Giotto::featStatsParam} on
a disk-backed expression store. One streamed pass over the triplet stream,
either over every cell or partitioned by a per-cell grouping.

The grouped form is reusable beyond QC: per-cluster mean and
percent-detected is the input to a dot plot, and the group means are a
pseudobulk matrix.

Passing \code{groups} also changes how the pass runs: the grouping is windowed
over cells, where the ungrouped form streams as a single plan — see
\code{\link[=storeChunkInfo]{storeChunkInfo()}} for the bound, and
\code{vignette("expression_windows", package = "GiottoDisk")} for why the two differ.
}
