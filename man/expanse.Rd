% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-expanse.R
\name{expanse}
\alias{expanse}
\alias{expanse,parquetGeomStore-method}
\alias{expanse,parquetGeomTileStore-method}
\title{Get the area of individual polygons}
\usage{
\S4method{expanse}{parquetGeomStore}(
  x,
  output = c("data.table", "named", "vector"),
  engine = c("terra", "sedona"),
  poly_id_col = "poly_ID",
  ...
)

\S4method{expanse}{parquetGeomTileStore}(
  x,
  output = c("data.table", "named", "vector"),
  engine = c("terra", "sedona"),
  poly_id_col = "poly_ID",
  ...
)
}
\arguments{
\item{x}{\code{parquetGeomStore} or \code{parquetGeomTileStore}}

\item{output}{one of \code{"data.table"} (default), \code{"named"}, or \code{"vector"}.
\code{"data.table"} returns a \code{data.table} with columns \code{cell_ID} and \code{area}.
\code{"named"} returns a named numeric vector with \code{cell_ID} as names.
\code{"vector"} returns a plain unnamed numeric vector — polygon identity is not
preserved.}

\item{engine}{one of \code{"terra"} (default) or \code{"sedona"}. \code{"sedona"} issues a
single \code{ST_Area(geom)} SQL query via SedonaDB/DataFusion without
materializing geometry in R. Pending affine transforms are applied via
\code{ST_Affine} before area computation, so results are correct on lazily
scaled or transformed stores. Requires the \code{sedonadb} package.}

\item{poly_id_col}{name of the polygon ID column (default \code{"poly_ID"})}

\item{...}{
  Arguments passed on to \code{\link[terra:expanse]{terra::expanse}}
  \describe{
    \item{\code{}}{}
  }}
}
\value{
depends on \code{output}: a \code{data.table}, named \code{numeric}, or \code{numeric}
}
\description{
Compute the area covered by polygons in a \code{parquetGeomStore}
or \code{parquetGeomTileStore}. The tiled terra path parallelizes across tiles
via \code{tileApply}; the sedona path issues a single \code{ST_Area} query regardless
of tiling.
}
\examples{
# create simple polygons
polys <- terra::vect(
    c("POLYGON ((0 0, 1 0, 1 1, 0 1, 0 0))",
      "POLYGON ((1 0, 3 0, 3 2, 1 2, 1 0))",
      "POLYGON ((0 0, 4 0, 4 4, 0 4, 0 0))")
)
terra::values(polys) <- data.frame(poly_ID = c("a", "b", "c"))

store <- storeCreate(type = "parquetgeom")
store <- storeWrite(store, polys)

# data.table output (default)
expanse(store)

# named vector
expanse(store, output = "named")

# sedona engine (requires sedonadb)
\dontrun{
if (requireNamespace("sedonadb", quietly = TRUE)) {
    expanse(store, engine = "sedona")
}
}
}
