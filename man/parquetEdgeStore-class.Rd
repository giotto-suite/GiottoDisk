% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-parquetEdgeStore.R
\name{parquetEdgeStore-class}
\alias{parquetEdgeStore-class}
\title{Parquet Edge Store (streaming)}
\description{
S4 class for \strong{disk-backed networks} stored as long-format Apache
Parquet. Carries a node-ID sidecar parquet for char<->int translation
so the in-memory class footprint stays bounded regardless of vertex
count.

Edge schema (locked):\tabular{lll}{
   Column \tab Type \tab Meaning \cr
   \code{from_id} \tab int32/64 \tab node int ID; canonical sort key \cr
   \code{to_id} \tab int32/64 \tab node int ID \cr
   \code{weight} \tab float32 \tab edge weight (e.g. sNN Jaccard) \cr
   \code{distance} \tab float32 \tab PCA / spatial Euclidean distance \cr
}


On-disk layout:

\if{html}{\out{<div class="sourceCode">}}\preformatted{<store_root>/                  # @path
├── edges/                     # arrow::open_dataset reads here
│   └── *.parquet              # single file today; hive-partitioned later
└── nodes/                     # auto-derived sidecar
    └── *.parquet
}\if{html}{\out{</div>}}

Both subdirs are read via \code{arrow::open_dataset}, which handles
file-or-dir transparently — extending to \verb{source_id=<uid>} hive
partitions for rbind support is a write-time change only.

Node sidecar schema (lives at \verb{<store_root>/nodes/}):\tabular{llll}{
   Column \tab Type \tab Required \tab Meaning \cr
   \code{node_id} \tab string \tab yes \tab original cell barcode / point ID \cr
   \code{int_id} \tab int32/64 \tab yes \tab dense integer enumeration \cr
   \code{*} \tab any \tab no \tab arbitrary writer-supplied metadata \cr
}


Optional sidecar columns survive round-trips and can be joined into
results at read time. Spatial coords (\code{x_index}, \code{y_index}) are NOT
duplicated here — they live in \code{parquetGeomStore} and are joined
across stores when needed.

For undirected networks (sNN, spatial) edges are stored once in
canonical form (\code{from_id <= to_id}). kNN networks are symmetrized
at write time and stored canonical. Algorithms requiring directed
traversal can union with a swap.
}
\section{Slots}{

\describe{
\item{\code{n_cells}}{numeric. Vertex count (= nrow nodes sidecar).}

\item{\code{n_edges}}{numeric. Edge count.}

\item{\code{nodes}}{parquetStore handle to the node sidecar.}

\item{\code{type}}{character. One of "kNN", "sNN", "spatial".}

\item{\code{directed}}{logical. TRUE = edges stored directed as-is;
FALSE = canonical undirected form.}
}}

\seealso{
\code{\link[=parquetEdgeStore]{parquetEdgeStore()}}

Other store types: 
\code{\link{binGefInput-class}},
\code{\link{cellbinGefInput-class}},
\code{\link{csvWideInput-class}},
\code{\link{dataStore-class}},
\code{\link{edgeInput-class}},
\code{\link{exprInput-class}},
\code{\link{fileStore-class}},
\code{\link{mtxInput-class}},
\code{\link{parquetExprStore-class}},
\code{\link{parquetStore-class}},
\code{\link{tenxH5Input-class}},
\code{\link{unionParquetExprStore-class}}
}
\concept{store types}
