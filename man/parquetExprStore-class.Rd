% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-parquetExprStore.R
\docType{class}
\name{parquetExprStore-class}
\alias{parquetExprStore-class}
\title{Parquet Expression Matrix Store (streaming)}
\description{
S4 class for \strong{disk-backed expression matrices} stored as long-format
Apache Parquet, designed for streaming read access. Unlike
\link[=parquetStore-class]{parquetStore} (which stores arbitrary tabular data with a
\code{row_index} column), \code{parquetExprStore} represents a sparse cell x gene
expression matrix using three integer / float columns:\tabular{lll}{
   Column \tab Type \tab Meaning \cr
   \code{row_id} \tab int32 \tab 1-based cell index \cr
   \code{col_id} \tab int32 \tab 1-based gene index \cr
   \code{value} \tab float64 \tab expression count \cr
}


Parquet files are sorted by \code{row_id} so that Arrow predicate-pushdown
row-group skipping makes chunked reads fast on large datasets.

Cell barcodes (\code{cell_IDs}) and gene names (\code{feat_IDs}) are stored as
character slots on the S4 object -- the Parquet payload itself stays
minimal. The slot vectors act as a lookup table from integer index to
character ID.
}
\section{Slots}{

\describe{
\item{\code{path}}{character. Local file path (single Parquet) or directory
(one Parquet per chunk for very large datasets). Arrow's \code{open_dataset}
handles both transparently.}

\item{\code{uid}}{character. Auto-generated unique ID for artifact tracking.}

\item{\code{read_fun}}{function. Preset to \code{arrow::open_dataset()}.}

\item{\code{params}}{list. Reserved for downstream pipeline metadata
(e.g. HVG indices after \code{sc_hvg}). Not used for normalization recipes
anymore — those live on \verb{@ops}.}

\item{\code{ops}}{list. Ordered chain of lazy arrow step recipes. Each entry
is a pure-data \code{list(type, ...params)} record (no closures). At
\code{storeRead()} time the executor \code{.pe_do_op()} translates each record
into composed arrow steps applied to the lazy query; the whole chain
compiles into one arrow plan executed once at collect / output
dispatch. Records survive \code{saveRDS} cleanly. Empty by default;
populated by \code{processData()} methods (libraryNorm, logNorm, ...).
See \code{R/utils-pestore-ops.R} for the op type registry.}

\item{\code{n_cells}}{numeric. Number of cells in the dataset
(length of \code{cell_ids}).}

\item{\code{n_genes}}{numeric. Number of genes / features
(length of \code{feat_ids}).}

\item{\code{cell_ids}}{character. Cell barcodes; index \code{i} corresponds to
\code{row_id == i} in the Parquet file.}

\item{\code{feat_ids}}{character. Gene / feature IDs; index \code{j} corresponds to
\code{col_id == j} in the Parquet file.}

\item{\code{chunk_size}}{numeric. Default Arrow read-chunk size in cells
(default 250,000).}

\item{\code{cell_idx}}{integer. Active cell positions in the original Parquet
(length 0 = no subset, all cells are active). When non-empty:
\verb{length(cell_idx) == length(cell_ids) == n_cells}.}

\item{\code{gene_idx}}{integer. Active gene positions in the original Parquet
(length 0 = no subset).}
}}

\section{Use case}{

This store is the streaming-friendly expression backend designed for the
scstream pipeline. It is slotted into \code{exprObj@exprMat} like any other
disk-backed store; downstream Giotto methods that recognize the class
will dispatch to streaming implementations.
}

\section{Subset semantics}{

\code{parquetExprStore} supports lightweight subsetting via the \code{[} operator.
\code{pe[i, j]} returns a new store whose \code{feat_ids} / \code{cell_ids} are narrowed
to the kept rows / columns and whose \code{gene_idx} / \code{cell_idx} slots record
the \emph{original} parquet positions of the kept entries. The Parquet file
on disk is \strong{not} rewritten -- \code{storeRead()} filters lazily via Arrow
using the recorded indices, so chained subsets stay cheap.
}

\seealso{
\code{\link[=parquetExprStore]{parquetExprStore()}}, \code{\link[=mtxInput]{mtxInput()}}

Other store types:
\code{\link{binGefInput-class}},
\code{\link{cellbinGefInput-class}},
\code{\link{csvWideInput-class}},
\code{\link{dataStore-class}},
\code{\link{edgeInput-class}},
\code{\link{exprInput-class}},
\code{\link{fileStore-class}},
\code{\link{mtxInput-class}},
\code{\link{parquetEdgeStore-class}},
\code{\link{parquetStore-class}},
\code{\link{tenxH5Input-class}},
\code{\link{unionParquetExprStore-class}}
}
\concept{store types}
