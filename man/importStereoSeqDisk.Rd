% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convenience-stereoseq.R
\name{importStereoSeqDisk}
\alias{importStereoSeqDisk}
\title{Import a BGI Stereo-seq assay (disk-backed)}
\usage{
importStereoSeqDisk(
  stereoseq_dir = NULL,
  backend,
  type = c("bin", "cell"),
  bin_size = "bin100",
  gene_column = c("geneName", "geneID"),
  negative_y = TRUE,
  gef_type
)
}
\arguments{
\item{stereoseq_dir}{Stereo-seq output directory}

\item{backend}{a \code{gsource} (typically \code{gDirSource}) project backend.
Naming matches \code{\link[GiottoClass:createGiottoObject]{GiottoClass::createGiottoObject()}}'s \code{backend} param.}

\item{type, bin_size, gene_column, negative_y, gef_type}{passed through to
the parent \code{StereoSeqReader} initializer.}
}
\value{
\code{StereoSeqDiskReader} object
}
\description{
Disk-backed counterpart to \code{\link[Giotto:importStereoSeq]{Giotto::importStereoSeq()}}. Produces a
\code{StereoSeqDiskReader} whose \code{load_expression()} call streams the
source \code{.gef} file into a \code{parquetExprStore} written to the
\code{gDirSource}-managed project vault. For \code{type = "bin"}, spatial
locations are derived from that same streaming pass. The remaining
modalities (images, masks, binpoints, polygons, and cellbin spatial
locations) come from the inherited \code{StereoSeqReader} closures.
}
\seealso{
\code{\link[Giotto:importStereoSeq]{Giotto::importStereoSeq()}} for the in-memory variant
}
