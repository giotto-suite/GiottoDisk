% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tilework.R
\name{getTile}
\alias{getTile}
\title{Get Tile Data}
\usage{
\S4method{getTile}{queryableStore,spatialTilePlan}(
  x,
  tiles,
  i = NULL,
  j,
  contiguous = FALSE,
  pad = NULL,
  get_params = list(),
  ...
)
}
\arguments{
\item{x}{\code{fileStore} object (currently \code{parquetStore} inheriting
only)}

\item{tiles}{{tilework} \verb{tile*} object}

\item{i}{\strong{ANY \verb{tile*} except tileIterator} tile vector index or row index
if \code{j} is also provided}

\item{j}{\strong{ANY \verb{tile*} except tileIterator} tile col index}

\item{contiguous}{\code{logical} (default = FALSE). Whether to retrieve tiles
using inclusivity rules (see \link{getBoundedData}) that prevent double counts
between neighboring tiles for values that fall on the boundary line.

When used, \code{get_params$inclusive} settings will be ignored.

This is mainly relevant only for padding = 0 situations with \code{tilePlan}
inheriting \code{tile} inputs and operations that will process all tiles in the
\code{tilePlan}. Inclusivity rules are calculated based on the entire set of tiles
defined in the \code{tilePlan}.}

\item{pad}{\code{numeric} (optional) additional padding to apply before tile retrieval.
Useful for temporarily increasing padding without affecting \verb{tile*} object.}

\item{get_params}{\code{list} (optional). Additional named params to pass to the
underlying \code{getBoundedData()} call.}

\item{...}{see additional params section}
}
\value{
a lazy arrow/dplyr query
}
\description{
Get the region of data defined by a {tilework} tile* object. This function
determines the tile(s) requested based on the tilePlan then calls
\code{\link[=getBoundedData]{getBoundedData()}} for the actual retrieval step.
}
\section{Additional params:}{
\itemize{
\item \code{advance} \strong{\code{tileIterator} only} \code{logical} (default = TRUE). Whether to
advance the iterator.
}
}

\seealso{
\link[tilework:tilePlan]{tilework::tilePlan}

Other tile methods: 
\code{\link{getBoundedData}()}
}
\concept{tile methods}
