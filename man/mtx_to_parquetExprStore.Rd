% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-parquetExprStore.R
\name{mtx_to_parquetExprStore}
\alias{mtx_to_parquetExprStore}
\title{Stream-convert a 10x / Xenium MatrixMarket triple into a parquetExprStore}
\usage{
mtx_to_parquetExprStore(
  mtx_path,
  barcodes_path,
  features_path,
  output_path,
  feature_id_col = 2L,
  batch_lines = 5000000L,
  overwrite = FALSE
)
}
\arguments{
\item{mtx_path}{character. Path to \code{matrix.mtx} or \code{matrix.mtx.gz}.
The first two non-comment columns are interpreted as
\verb{(gene_index, cell_index)} per the 10x / Xenium convention.}

\item{barcodes_path}{character. Path to \code{barcodes.tsv} or
\code{barcodes.tsv.gz} — one barcode per line; length must equal the
number of columns in the mtx (n_cells).}

\item{features_path}{character. Path to \code{features.tsv} or
\code{features.tsv.gz}. By default takes column 2 (gene symbol) as
\code{feat_id}; pass \code{feature_id_col = 1L} to use the Ensembl id.}

\item{output_path}{character. Destination Parquet file (single file,
used when \code{nnz <= batch_lines}) or directory (created if missing,
one Parquet per batch).}

\item{feature_id_col}{integer. Which column of \code{features.tsv} to use as
the gene identifier. Default \code{2L} (gene symbol).}

\item{batch_lines}{integer. Number of mtx triplet lines processed per
batch. Default 5,000,000 — keeps peak RAM around ~120 MB per batch.}

\item{overwrite}{logical. If \code{TRUE}, replace \code{output_path} if it exists.}
}
\value{
A \linkS4class{parquetExprStore} object pointing at \code{output_path} with
\code{cell_ids} / \code{feat_ids} populated.
}
\description{
Reads a MatrixMarket sparse matrix (\code{.mtx} or \code{.mtx.gz}) line by line in
batches, swaps the (gene-row, cell-col) orientation to the streaming
convention (\code{row_id = cell}, \code{col_id = gene}), sorts each batch by
\code{row_id}, and writes one Parquet file per batch. \strong{No dense or sparse
matrix is ever materialized in memory} — only \code{batch_lines} triplet
rows are held at a time.

The 10x / Xenium MatrixMarket output is already column-major
(cell-sorted), so each batch is internally cell-sorted and Arrow's
file-level statistics enable predicate pushdown across the directory of
Parquet chunks.
}
\seealso{
\code{\link[=xenium_to_parquetExprStore]{xenium_to_parquetExprStore()}} (convenience wrapper for the
\verb{cell_feature_matrix/} directory layout).
}
