% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-fileInputs.R
\name{mtxInput}
\alias{mtxInput}
\title{Create a 10x / Xenium MatrixMarket triple input}
\usage{
mtxInput(
  mtx_path,
  barcodes_path = NULL,
  features_path = NULL,
  cell_ids = NULL,
  feat_ids = NULL,
  feature_id_col = 2L,
  batch_lines = 5000000L
)
}
\arguments{
\item{mtx_path}{character. Path to a directory (10x layout) or a
\code{matrix.mtx[.gz]} file.}

\item{barcodes_path}{character. Path to \code{barcodes.tsv[.gz]}. Default:
auto-resolved relative to \code{mtx_path}. Ignored if \code{cell_ids} is
supplied.}

\item{features_path}{character. Path to \code{features.tsv[.gz]}. Default:
auto-resolved relative to \code{mtx_path}. Ignored if \code{feat_ids} is
supplied.}

\item{cell_ids}{character. Explicit cell barcodes. If supplied,
\code{barcodes_path} is not consulted.}

\item{feat_ids}{character. Explicit feature IDs. If supplied,
\code{features_path} and \code{feature_id_col} are not consulted.}

\item{feature_id_col}{integer. \code{1L} = Ensembl ID, \code{2L} = gene symbol
(default). Used only when reading from \code{features_path}.}

\item{batch_lines}{integer. Triplets per batch. Default 5,000,000.}
}
\value{
An \code{mtxInput} object.
}
\description{
Eagerly reads \code{barcodes.tsv} and \code{features.tsv} so \code{cell_ids},
\code{feat_ids}, \code{n_cells}, \code{n_genes} are known up-front. The mtx file
itself is not opened until \code{\link[=storeRead]{storeRead()}}.

Three input modes are supported:
\enumerate{
\item \strong{Directory} (10x / Xenium \verb{cell_feature_matrix/} layout):
\code{mtxInput(dir)} — auto-resolves \code{matrix.mtx[.gz]}, \code{barcodes.tsv[.gz]},
\code{features.tsv[.gz]} inside the directory.
\item \strong{Matrix file with sidecars resolvable in its parent}:
\code{mtxInput(mtx_path)} — used when the three files live as siblings.
\item \strong{Bare \code{.mtx} with explicit IDs}:
\code{mtxInput(mtx_path, cell_ids = ..., feat_ids = ...)} — for non-10x
MatrixMarket files where the barcodes/features sidecars aren't
present. \code{cell_ids} and \code{feat_ids} must both be supplied.
}

Explicit \code{cell_ids} / \code{feat_ids} always override sidecar resolution.
}
\seealso{
Other store constructors: 
\code{\link{binGefInput}},
\code{\link{cellbinGefInput}},
\code{\link{csvWideInput}},
\code{\link{fileStore}},
\code{\link{parquetExprStore}},
\code{\link{parquetStore}},
\code{\link{storeCreate}()},
\code{\link{tenxH5Input}},
\code{\link{unionParquetExprStore}}
}
\concept{store constructors}
