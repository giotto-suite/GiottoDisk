% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convenience-merscope.R
\name{importMerscopeDisk}
\alias{importMerscopeDisk}
\title{Import a Vizgen MERSCOPE assay (disk-backed)}
\usage{
importMerscopeDisk(
  merscope_dir = NULL,
  backend,
  score_threshold = NULL,
  polygon_format = c("parquet", "hdf5")
)
}
\arguments{
\item{merscope_dir}{MERSCOPE region directory (the one containing
\code{detected_transcripts.parquet}). \code{experiment.json} is looked up one level
above, where the vendor writes it.}

\item{backend}{a \code{gsource} (typically \code{gDirSource}) project backend.}

\item{score_threshold}{\code{numeric} or \code{NULL} (default). Minimum
\code{transcript_score} retained. Vendor exports are already score-filtered, so
unlike Xenium's \code{qv_threshold} this defaults to no additional filtering.}

\item{polygon_format}{\code{character}. \code{"parquet"} (default) or \code{"hdf5"}.}
}
\value{
\code{MerscopeDiskReader} object
}
\description{
Disk-backed MERSCOPE reader. Produces a \code{MerscopeDiskReader} whose
\code{load_transcripts()}, \code{load_polys()} and \code{load_expression()} calls write to a
\code{gDirSource}-managed project vault. Cell metadata remains in memory.

Unlike \code{\link[=importXeniumDisk]{importXeniumDisk()}}, this reader does not subclass an in-memory
{Giotto} reader -- no \code{MerscopeReader} S4 class exists -- so it performs its
own path detection.
}
\seealso{
\code{\link[=importXeniumDisk]{importXeniumDisk()}}
}
