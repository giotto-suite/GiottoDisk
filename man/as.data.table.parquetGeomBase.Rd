% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-ops.R
\name{as.data.table.parquetGeomBase}
\alias{as.data.table.parquetGeomBase}
\title{as.data.table for parquetGeomBase}
\usage{
\method{as.data.table}{parquetGeomBase}(
  x,
  keep.rownames = FALSE,
  ...,
  geom = "",
  include_values = TRUE,
  geomtype = NULL
)
}
\arguments{
\item{x}{a \code{parquetGeomBase} store}

\item{keep.rownames}{unused; kept for S3 generic conformance}

\item{...}{passed to \code{storeRead}}

\item{geom}{one of \code{""}, \code{"wkb"}, \code{"XY"}}

\item{include_values}{when \code{geom = "XY"}, include attribute columns
alongside vertex coordinates. Mirrors \code{as.data.table} / \code{as.data.frame}
on a terra \code{SpatVector}.}

\item{geomtype}{unused; kept for SpatVector signature conformance}
}
\value{
a \code{data.table}
}
\description{
Materialize a \code{parquetGeomBase} (parquetGeomStore or
parquetGeomTileStore) to a \code{data.table}. Mirrors \code{terra}'s
\code{as.data.table} signature for \code{SpatVector}:
\itemize{
\item \code{geom = ""} (default): attributes only, no geometry column.
\item \code{geom = "wkb"}: include a \code{geom} column with raw WKB blobs.
\item \code{geom = "XY"}: expand to one row per vertex with \code{x}, \code{y}, plus
\code{feature_id} / \code{part_id} / \code{ring_id} for grouping in plot consumers
(e.g. \code{ggplot2::geom_polygon(aes(group = feature_id))}).
}

The \code{geom = "XY"} path goes through \code{wk::wk_coords} — no terra
\code{SpatVector} intermediate, no GDAL allocation. Requires the \code{wk}
package (Suggests).

Registered as an S3 method on the virtual \code{parquetGeomBase} class so
\code{data.table::as.data.table}'s class-chain iteration picks it up for
the concrete subclasses (\code{parquetGeomStore}, \code{parquetGeomTileStore},
\code{unionParquetGeomStore}).
}
