% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-parquetExprStore.R
\name{parquetExprStore}
\alias{parquetExprStore}
\title{Create a Parquet Expression Matrix Store}
\usage{
parquetExprStore(
  path = .dump_tempfile(),
  cell_ids = character(0L),
  feat_ids = character(0L),
  n_cells = length(cell_ids),
  n_genes = length(feat_ids),
  chunk_size = 250000,
  ...
)
}
\arguments{
\item{path}{character. Path to a Parquet file or directory of Parquet
chunks.}

\item{cell_ids}{character. Cell barcodes. Length must equal \code{n_cells}.}

\item{feat_ids}{character. Gene / feature IDs. Length must equal
\code{n_genes}.}

\item{n_cells}{numeric. Total number of cells. Defaults to
\code{length(cell_ids)}.}

\item{n_genes}{numeric. Total number of genes. Defaults to
\code{length(feat_ids)}.}

\item{chunk_size}{numeric. Default Arrow read-chunk size in cells
(default 250,000).}

\item{...}{additional slots passed through to \code{new()}.}
}
\value{
A \code{parquetExprStore} S4 object.
}
\description{
Construct a \linkS4class{parquetExprStore} handle around a long-format Parquet
file or directory.  The Parquet must contain \code{row_id}, \code{col_id}, \code{value}
columns and be sorted by \code{row_id}.

To create the Parquet from a 10x / Xenium MatrixMarket triple, use
\code{\link[=mtx_to_parquetExprStore]{mtx_to_parquetExprStore()}}.
}
\seealso{
Other store constructors: 
\code{\link{fileStore}},
\code{\link{parquetStore}},
\code{\link{storeCreate}()}
}
\concept{store constructors}
