% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-parquetExprStore.R
\name{parquetExprStore}
\alias{parquetExprStore}
\title{Create a Parquet Expression Matrix Store}
\usage{
parquetExprStore(
  path = .dump_tempfile(),
  cell_ids = character(0L),
  feat_ids = character(0L),
  n_cells = length(cell_ids),
  n_genes = length(feat_ids),
  scan_stats = FALSE,
  ...
)
}
\arguments{
\item{path}{character. Path to a Parquet file or directory of Parquet
chunks.}

\item{cell_ids}{character. Cell barcodes. Length must equal \code{n_cells}.}

\item{feat_ids}{character. Gene / feature IDs. Length must equal
\code{n_genes}.}

\item{n_cells}{numeric. Total number of cells. Defaults to
\code{length(cell_ids)}.}

\item{n_genes}{numeric. Total number of genes. Defaults to
\code{length(feat_ids)}.}

\item{scan_stats}{logical. Scan the Parquet at \code{path} to cache its marginal
nonzero counts on \verb{@stats} (default \code{FALSE}). Only meaningful when \code{path}
already holds data: the usual pattern is to construct an empty handle and
populate it with \code{\link[=storeWrite]{storeWrite()}}, which caches the marginals itself. Set
\code{TRUE} when attaching to a Parquet written elsewhere and you would rather
pay the scan now than have the first consumer pay it. Leaving it \code{FALSE}
costs correctness nothing — consumers that need the counts fall back to
counting on demand — so this is purely about when the scan happens.
Reachable through \code{\link[=storeCreate]{storeCreate()}}, which forwards \code{...} here.}

\item{...}{additional slots passed through to \code{new()}.}
}
\value{
A \code{parquetExprStore} S4 object.
}
\description{
Construct a \link[=parquetExprStore-class]{parquetExprStore} handle around a long-format Parquet
file or directory.  The Parquet must contain \code{row_id}, \code{col_id}, \code{value}
columns and be sorted by \code{row_id}.

To populate the Parquet from a 10x / Xenium MatrixMarket triple, build
an \code{\link[=mtxInput]{mtxInput()}} and pass it to \code{\link[=storeWrite]{storeWrite()}}.
}
\seealso{
Other store constructors:
\code{\link[=binGefInput]{binGefInput()}},
\code{\link[=cellbinGefInput]{cellbinGefInput()}},
\code{\link[=csvWideInput]{csvWideInput()}},
\code{\link[=edgeDTInput]{edgeDTInput()}},
\code{\link[=fileStore]{fileStore()}},
\code{\link[=igraphInput]{igraphInput()}},
\code{\link[=mtxInput]{mtxInput()}},
\code{\link[=nnSearchInput]{nnSearchInput()}},
\code{\link[=parquetEdgeStore]{parquetEdgeStore()}},
\code{\link[=parquetStore]{parquetStore()}},
\code{\link[=storeCreate]{storeCreate()}},
\code{\link[=tenxH5Input]{tenxH5Input()}},
\code{\link[=tenxZarrInput]{tenxZarrInput()}},
\code{\link[=unionParquetExprStore]{unionParquetExprStore()}}
}
\concept{store constructors}
