% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convenience-visiumHD.R
\name{importVisiumHDDisk}
\alias{importVisiumHDDisk}
\title{Import a 10x VisiumHD assay (disk-backed)}
\usage{
importVisiumHDDisk(
  visiumhd_dir = NULL,
  backend,
  bin = 8,
  micron = FALSE,
  outdir = NULL,
  expression_source = "raw",
  feature_id_type = c("symbol", "ensembl"),
  tissue_only = FALSE,
  barcodes = NULL,
  array_subset_row = NULL,
  array_subset_col = NULL,
  pxl_subset_row = NULL,
  pxl_subset_col = NULL,
  filter = NULL,
  filter_coverage_cutoff = 0.5
)
}
\arguments{
\item{visiumhd_dir}{VisiumHD output directory}

\item{backend}{a \code{gsource} (typically \code{gDirSource}) project backend.
Naming matches \code{\link[GiottoClass:create_giotto]{GiottoClass::createGiottoObject()}}'s \code{backend} param.}

\item{bin, micron, outdir, expression_source, feature_id_type, tissue_only, barcodes, array_subset_row, array_subset_col, pxl_subset_row, pxl_subset_col, filter, filter_coverage_cutoff}{passed through to the parent \code{VisiumHDReader} initializer.}
}
\value{
\code{VisiumHDDiskReader} object
}
\description{
Disk-backed counterpart to \code{\link[Giotto:importVisiumHD]{Giotto::importVisiumHD()}}. Produces a
\code{VisiumHDDiskReader} whose \code{load_expression()} call writes a
\code{parquetExprStore} into a \code{gDirSource}-managed project vault.
Other modalities (tissue positions, scalefactors, images, polygons,
transcripts, cellmeta) remain in-memory via the inherited
\code{VisiumHDReader} closures.
}
\seealso{
\code{\link[Giotto:importVisiumHD]{Giotto::importVisiumHD()}} for the in-memory variant
}
