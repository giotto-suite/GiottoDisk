% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-snapshotSave.R
\name{snapshotSave}
\alias{snapshotSave}
\alias{snapshotSave,gDirSource,giotto-method}
\title{Write Giotto Snapshot}
\usage{
\S4method{snapshotSave}{gDirSource,giotto}(
  src,
  x,
  name = format(Sys.time(), "\%Y\%m\%d_giottosave"),
  method = c("rds", "qs"),
  method_params = list(),
  overwrite = FALSE,
  export_image = TRUE,
  verbose = NULL,
  ...
)
}
\arguments{
\item{src}{\code{gsource} object. Used to provide default save locations
and save formats for a managed Giotto backend.}

\item{x}{\code{giotto} object to write}

\item{method}{\code{character} (default = "rds").}

\item{method_params}{\code{list} (optional) list of additional named params for
save method.}

\item{overwrite}{\code{logical} (default = FALSE). Whether to overwrite if a
snapshot of the same name exists.}

\item{export_image}{\code{logical} (default = TRUE) Whether to make a copy of
external images in the project directory}

\item{verbose}{verbosity}

\item{...}{additional params to pass (none implemented)}
}
\value{
the modified gobject (invisible). Mutated by the internal
adoption pass — captured by \code{snapshotSave(gDirSource, giottoMulti)}
so the multi-level \code{.rds} sees post-adoption file handles in each
child.
}
\description{
Register a gobject snapshot into the source
}
