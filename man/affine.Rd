% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-transforms.R
\name{affine}
\alias{affine}
\title{Lazily record an affine transform on a parquetGeomBase store}
\usage{
\S4method{affine}{parquetGeomBase,affine2d}(x, y, ...)
}
\arguments{
\item{x}{\code{parquetGeomBase}-inheriting store}

\item{y}{\code{affine2d} transform object (from GiottoClass)}

\item{...}{additional arguments (ignored)}
}
\value{
\code{x} with the transform recorded in \verb{@post_ops}
}
\description{
Record an affine transformation on a \code{parquetGeomBase}-inheriting store. The
transform is stored as a \code{"transform"} entry in \verb{@post_ops} and applied to output
coordinates at materialization time (\code{storeRead(output = "tibble"/"terra"/"sf")}).
It is a no-op for the Arrow query phase.

Subsequent \code{affine()} calls compose with any existing pending transform -- the
chain collapses to a single \code{affine2d} entry. Spatial filters
(\code{\link[=crop]{crop()}}/\code{\link{window<-}}) applied after an \code{affine()} call back-project their
query extents to intrinsic (on-disk) space for accurate Arrow filtering.

\code{output = "query"} and \code{output = "duckdb"} are unaffected.
}
