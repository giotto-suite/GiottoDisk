% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-storeWrite.R
\name{storeWrite-parquetGeomStore}
\alias{storeWrite-parquetGeomStore}
\alias{storeWrite,parquetGeomStore,data.frame-method}
\title{Write to a Parquet Geometry Storage}
\usage{
\S4method{storeWrite}{parquetGeomStore,SpatVector}(
  store,
  data,
  meta = NULL,
  row_offset = 0,
  tile_idx = 0L,
  split_geom = FALSE,
  split_geom_fmt = "poly_\%d",
  split_geom_sourcename = NULL,
  .arrow_meta = NULL,
  ...
)

\S4method{storeWrite}{parquetGeomStore,data.frame}(
  store,
  data,
  type = c("points", "polygons"),
  id_col,
  sdimx,
  sdimy,
  part_col = NULL,
  row_offset = 0,
  ...
)
}
\arguments{
\item{store}{\code{dataStore} inheriting class}

\item{data}{data to write}

\item{meta}{(optional) \code{data.frame-like} that contains attributes info in the
same ordering as the spatvector in \code{data}. If not provided and attributes
are in the spatvector, they will be automatically extracted with
\code{\link[terra:values]{terra::values()}}}

\item{row_offset}{\code{numeric}, will be coerced to \code{integer} (default = 0L).
Offset to apply to row number indexing. For example \code{row_offset = 0L} means
the first \code{row_index} value in this file will be \code{1}.}

\item{tile_idx}{\code{integer(1)}. Tile index to write under as a hive partition
(\verb{tile_index=<n>/}). Defaults to \code{0L} so flat geom stores share a uniform
\verb{(tile_index, row_index)} join key with tiled stores without needing
\code{dplyr::coalesce}. Pass \code{NULL} to write without a tile subdir (not
recommended for \code{parquetGeomStore}).}

\item{split_geom}{\code{logical} (polygons only, default \code{FALSE}). When \code{TRUE},
multipart polygons are split into single-part polygons via
\code{\link[GiottoClass:splitGeom]{GiottoClass::splitGeom()}} before write. Caller is responsible for
producing globally unique IDs across all writes; tile orchestration
layers (the \code{parquetGeomTileStore} methods) bake the tile index into
\code{split_geom_fmt} before dispatch.}

\item{split_geom_fmt}{\code{character}. \code{sprintf} format with a single \verb{\%d} slot
for the row counter. Default \code{"poly_\%d"}. See \code{\link[GiottoClass:splitGeom]{splitGeom()}}.}

\item{split_geom_sourcename}{\code{character} or \code{NULL}. Column name carrying
the original \code{poly_ID} values (duplicated per part of a multipart
polygon). Default \code{NULL}.}

\item{.arrow_meta}{\verb{named list} of \code{character} strings to add as parquet
metadata}

\item{...}{addtional params to pass to \code{parquetStore} method and
\code{\link[arrow:write_dataset]{arrow::write_dataset()}}}

\item{type}{\code{character}. Either \code{"points"} or \code{"polygons"}. What type of
geometry to write.}

\item{id_col}{\code{character}. Column name in \code{data} that holds the geometry
identifier. For polygons this groups vertices into individual geometries;
for points this is the feature name. Regardless of the input name, the
column is written to disk as \code{poly_ID} (polygons) or \code{feat_ID} (points)
so downstream consumers (e.g. \code{\link[=calculateOverlap]{calculateOverlap()}}) can assume standardized
names.}

\item{sdimx}{\code{character}. Column name containing x spatial coordinates.}

\item{sdimy}{\code{character}. Column name containing y spatial coordinates.}

\item{part_col}{\code{character} (optional, polygons only). Column name for
multi-part polygon grouping. See \link[GiottoClass:createGiottoPolygon]{GiottoClass::createGiottoPolygon}.}
}
\description{
Write geometry data to a \code{parquetGeomStore}. Each row is a single geometry.
Enforces the presence of \code{x_index} and \code{y_index} cols which are the centroid
(as calculated by the center of the geometry's spatial extent) and the
\code{row_index} which preserves row ordering.

May be written either directly from terra \code{SpatVector} or \code{data.frame} inputs
that are first parsed via GiottoClass \code{createGiottoPoints} or
\code{createGiottoPolygon}, depending on the \code{type} param.
}
\seealso{
Other storeWrite methods:
\code{\link{storeWrite,parquetEdgeStore,edgeInput-method}},
\code{\link{storeWrite-parquetGeomTileStore}},
\code{\link{storeWrite-parquetStore}}
}
\concept{storeWrite methods}
