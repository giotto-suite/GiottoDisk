% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convenience-merscope.R
\name{createGiottoMerscopeObjectDisk}
\alias{createGiottoMerscopeObjectDisk}
\title{Create a disk-backed MERSCOPE Giotto object}
\usage{
createGiottoMerscopeObjectDisk(
  merscope_dir,
  backend,
  score_threshold = NULL,
  polygon_format = c("parquet", "hdf5"),
  data_to_use = c("subcellular", "aggregate"),
  poly_z_indices = NULL,
  FOVs = NULL,
  flip_axis = c("none", "y", "x", "both"),
  split_keyword = list("Blank"),
  spat_unit = "cell",
  load_transcripts = TRUE,
  load_expression = TRUE,
  load_cellmeta = FALSE,
  instructions = NULL,
  verbose = NULL
)
}
\arguments{
\item{merscope_dir}{MERSCOPE region directory}

\item{backend}{a \code{gsource} project backend}

\item{score_threshold}{minimum \code{transcript_score}, or \code{NULL} (default)}

\item{polygon_format}{\code{"parquet"} (default) or \code{"hdf5"}}

\item{data_to_use}{\code{"subcellular"} (default) builds from transcripts and
polygons; \code{"aggregate"} uses only the vendor cell x gene matrix.}

\item{poly_z_indices}{which z-indices to use for polygons. \code{NULL} (default)
auto-detects the architecture -- see the z-plane note in this file.}

\item{FOVs}{which FOVs to load. \code{NULL} (default) loads all.}

\item{flip_axis}{axis along which to reflect polygons and transcripts after
loading: \code{"none"} (default), \code{"y"}, \code{"x"}, \code{"both"}. The reflection origin
is the midpoint of the transcript coordinate range so both layers stay
registered.}

\item{split_keyword}{keywords separating feature detections. Default
\code{list("Blank")}, the MERSCOPE negative-control prefix.}

\item{spat_unit}{name for the cell spatial unit (default \code{"cell"})}

\item{load_transcripts, load_expression, load_cellmeta}{logical}

\item{instructions}{Giotto instructions}

\item{verbose}{verbosity}
}
\value{
a \code{giotto} object backed by \code{backend}
}
\description{
One-call entry point mirroring the shape of
\code{Giotto::createGiottoXeniumObject(backend = )}, for Vizgen MERSCOPE output.
}
