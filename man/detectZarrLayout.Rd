% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zarr-detect.R
\name{detectZarrLayout}
\alias{detectZarrLayout}
\title{Detect the zarr layout of a Xenium/Atera output directory}
\usage{
detectZarrLayout(dir)
}
\arguments{
\item{dir}{Xenium/Atera output directory}
}
\value{
list with \code{layout_version} (\code{NA} when the directory holds no
zarr products), \code{products} (named paths for \code{cells}, \code{transcripts},
\code{cell_feature_matrix}, \code{analysis}; \code{NULL} when absent),
\code{gene_panel_json}, and \code{experiment_info}.
}
\description{
Inspect an output directory for Xenium/Atera \code{.zarr.zip} archives (or
unzipped \code{.zarr} trees) and return a layout descriptor naming the
detected layout version and per-product paths. Unsupported layouts
(zarr v3 stores, or archives missing the expected internal structure)
error with a message naming what was found.
}
\examples{
\dontrun{
detectZarrLayout("/path/to/xenium_output")
}
}
