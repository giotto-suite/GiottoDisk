% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-fileInputs.R
\name{tenxZarrInput}
\alias{tenxZarrInput}
\title{Create a 10x Xenium/Atera zarr expression input}
\usage{
tenxZarrInput(
  zarr_path,
  feature_id_col = 2L,
  mode = c("auto", "full", "cellblock"),
  cells_per_block = NULL,
  drop_aggregate = TRUE
)
}
\arguments{
\item{zarr_path}{character. Path to \code{cell_feature_matrix.zarr.zip} or
an unzipped \code{cell_feature_matrix.zarr} directory.}

\item{feature_id_col}{integer. \code{1L} = Ensembl ID (zarr \code{feature_ids}),
\code{2L} = gene symbol (zarr \code{feature_keys}; default). Mirrors
\code{\link[=mtxInput]{mtxInput()}}'s features.tsv column semantics.}

\item{mode}{character. "auto" (default; pick from the RAM budget),
"full" (two-pass cell-major placement over full triplet buffers), or
"cellblock" (bounded per-cell-block rescans).}

\item{cells_per_block}{integer. Cells per window in cellblock mode.
\code{NULL} (default) derives it from the RAM budget at read time.}

\item{drop_aggregate}{logical. Drop \code{aggregate_gene} summary features
(the "Total transcripts" row) from the matrix (default \code{TRUE}).}
}
\value{
A \code{tenxZarrInput} object.
}
\description{
Eagerly reads the feature catalog, cell barcodes and \code{indptr} from a
\code{cell_feature_matrix.zarr.zip} archive so \code{cell_ids} / \code{feat_ids} /
\code{n_cells} / \code{n_genes} are known up-front; \code{indices} / \code{data} are
streamed by \code{\link[=storeRead]{storeRead()}}.
}
\seealso{
Other store constructors:
\code{\link[=binGefInput]{binGefInput()}},
\code{\link[=cellbinGefInput]{cellbinGefInput()}},
\code{\link[=csvWideInput]{csvWideInput()}},
\code{\link[=edgeDTInput]{edgeDTInput()}},
\code{\link[=fileStore]{fileStore()}},
\code{\link[=igraphInput]{igraphInput()}},
\code{\link[=mtxInput]{mtxInput()}},
\code{\link[=nnSearchInput]{nnSearchInput()}},
\code{\link[=parquetEdgeStore]{parquetEdgeStore()}},
\code{\link[=parquetExprStore]{parquetExprStore()}},
\code{\link[=parquetStore]{parquetStore()}},
\code{\link[=storeCreate]{storeCreate()}},
\code{\link[=tenxH5Input]{tenxH5Input()}},
\code{\link[=unionParquetExprStore]{unionParquetExprStore()}}
}
\concept{store constructors}
