% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-ops.R
\name{subset}
\alias{subset}
\alias{subset,parquetBase-method}
\title{Subset a parquet store}
\usage{
\S4method{subset}{parquetBase}(x, subset, select, negate = FALSE, quote = TRUE, ...)
}
\arguments{
\item{x}{\code{parquetBase}-inheriting store object}

\item{subset}{logical \code{expression} to filter rows. Column names
in the store are referenced directly. Local variables are
inlined automatically -- no \verb{!!} injection needed.}

\item{select}{\code{expression}, indicating columns to keep. \code{-} can
be used to drop columns.}

\item{negate}{\code{logical}. If \code{TRUE}, the filter expression is
negated, keeping rows/cols that do NOT satisfy the condition.}

\item{quote}{\code{logical}. If \code{TRUE} (default), \code{subset} is captured
via NSE. If \code{FALSE}, \code{subset} is treated as a pre-built R \code{call}
object, allowing programmatic construction of filter expressions.}

\item{...}{additional arguments (ignored)}
}
\value{
the store with the filter step appended to \verb{@ops}
}
\description{
Filter rows of a parquet store by a logical expression. The
operation is recorded lazily in the store's \verb{@ops} slot and
applied at read time.

Expressions are evaluated using non-standard evaluation. Local
variables referenced in the expression are automatically inlined
at capture time, making the recorded operation self-contained
and safe to pass across sessions or to parallel workers.
}
\examples{
# standard NSE
subset(store, gene == "EPCAM")

# local variable -- inlined automatically
my_genes <- c("EPCAM", "CDH1")
subset(store, gene \%in\% my_genes)

# negation
subset(store, gene == "EPCAM", negate = TRUE)

# pre-built expression
expr <- quote(gene == "EPCAM")
subset(store, expr, quote = FALSE)
}
