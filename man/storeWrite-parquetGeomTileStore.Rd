% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-storeWrite.R
\name{storeWrite-parquetGeomTileStore}
\alias{storeWrite-parquetGeomTileStore}
\title{Write to a Parquet Geometry Tiled Storage}
\usage{
\S4method{storeWrite}{parquetGeomTileStore,queryableStore}(
  store,
  data,
  n_tiles = 100,
  i = NULL,
  sdimx,
  sdimy,
  type = c("points", "polygons"),
  contiguous = TRUE,
  dry_run = FALSE,
  geom_param = list(verbose = FALSE),
  write_param = list(),
  verbose = NULL,
  ...
)

\S4method{storeWrite}{parquetGeomTileStore,parquetStore}(
  store,
  data,
  n_tiles = 100,
  i = NULL,
  sdimx,
  sdimy,
  group_col = NULL,
  type = c("points", "polygons"),
  contiguous = TRUE,
  dry_run = FALSE,
  geom_param = list(verbose = FALSE),
  write_param = list(),
  verbose = NULL,
  ...
)

\S4method{storeWrite}{parquetGeomTileStore,parquetGeomStore}(
  store,
  data,
  n_tiles = 100,
  i = NULL,
  contiguous = TRUE,
  dry_run = FALSE,
  write_param = list(),
  verbose = NULL,
  ...
)
}
\arguments{
\item{store}{\code{dataStore} inheriting class}

\item{data}{data to write}

\item{n_tiles}{\code{numeric}. Minimum number of tiles to write across}

\item{i}{\code{integerlike} specific tile(s) to write. Leave as NULL (default)
to write all.}

\item{sdimx, sdimy}{\code{character}. Names of columns containing x and y values,
respectively.}

\item{type}{\code{character}. Either \code{"points"} or \code{"polygons"}. What type of
geometry to write. Determines whether \code{\link[GiottoClass:createGiottoPoints]{GiottoClass::createGiottoPoints()}}
or \code{\link[GiottoClass:createGiottoPolygon]{GiottoClass::createGiottoPolygon()}} is used to parse the values.}

\item{contiguous}{\code{logical} (default = TRUE) When \code{TRUE}, tile inclusivity
rules (see \link{getBoundedData}) prevent duplication of data at tile boundaries
(assuming no padding).}

\item{dry_run}{\code{logical} (default = FALSE). When \code{TRUE}, stops after planning
the tiles to write and plots the tiles to write.}

\item{geom_param}{\code{list} (optional). Additional params to pass to
\link[GiottoClass:createGiottoPoints]{GiottoClass::createGiottoPoints} or \link[GiottoClass:createGiottoPolygon]{GiottoClass::createGiottoPolygon},
depending on \code{type}.}

\item{write_param}{named \code{list} (optional). Additional params to pass to the
writing function.}

\item{...}{additional params to pass to \code{tileApply}}

\item{group_col}{\code{character}. If \code{type = "polygons"}, name of column that
groups related rows (vertices). Usually the polygon IDs.}
}
\description{
Write geometry data to hive-partitoned spatially tiled parquets. The partition
column is \code{"tile_index"}.
\subsection{Accepted \code{data} inputs}{
\itemize{
\item \code{queryableStore}-inheriting - Any dplyr lazy queryable store. Requires
selection of \code{type} param.
\itemize{
\item \code{"points"} - written directly.
\item \code{"polygons"} - must already be written to \code{"parquetStore"}-inheriting
class. Computes geometries from vertices which requires row ordering.
}
\item \code{parquetGeomStore}-inheriting - Simple write to a new tiled store.
}
}
}
\seealso{
Other storeWrite methods: 
\code{\link{storeWrite}()},
\code{\link{storeWrite-parquetGeomStore}},
\code{\link{storeWrite-parquetStore}}
}
\concept{storeWrite methods}
