% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-storeWrite.R
\name{storeWrite-parquetGeomTileStore}
\alias{storeWrite-parquetGeomTileStore}
\title{Write to a Parquet Geometry Tiled Storage}
\usage{
\S4method{storeWrite}{parquetGeomTileStore,queryableStore}(
  store,
  data,
  threshold = NULL,
  tiles = NULL,
  i = NULL,
  id_col,
  sdimx,
  sdimy,
  type = c("points", "polygons"),
  contiguous = TRUE,
  dry_run = FALSE,
  write_param = list(),
  verbose = NULL,
  ...
)

\S4method{storeWrite}{parquetGeomTileStore,parquetStore}(
  store,
  data,
  threshold = NULL,
  tiles = NULL,
  i = NULL,
  id_col,
  sdimx,
  sdimy,
  part_col = NULL,
  group_col = id_col,
  type = c("points", "polygons"),
  contiguous = TRUE,
  dry_run = FALSE,
  write_param = list(),
  verbose = NULL,
  ...
)

\S4method{storeWrite}{parquetGeomTileStore,parquetGeomStore}(
  store,
  data,
  threshold = NULL,
  tiles = NULL,
  i = NULL,
  contiguous = TRUE,
  dry_run = FALSE,
  write_param = list(),
  verbose = NULL,
  ...
)
}
\arguments{
\item{store}{\code{dataStore} inheriting class}

\item{data}{data to write}

\item{threshold}{\code{numeric} or \code{NULL}. Maximum number of records per tile.
\code{NULL} (default) auto-selects based on total row count and \code{type}:
\itemize{
\item \code{"points"}: 1M/tile up to 1B rows; clamped ramp \code{n/1000} (1M–100M) above.
\item \code{"polygons"} (vertex rows): 500k/tile up to 500M rows;
clamped ramp \code{n/1000} (500k–5M) above that.
}}

\item{tiles}{\code{tilePlan} or \code{NULL}. Optional seed tile plan passed to
\code{\link[tilework:quadtreePlan]{tilework::quadtreePlan()}} as the initial grid. When \code{NULL}, \code{quadtreePlan}
builds a default grid based on the data extent's aspect ratio.}

\item{i}{\code{integerlike} specific tile(s) to write. Leave as NULL (default)
to write all.}

\item{id_col}{\code{character}. Column name to use as the geometry identifier.
For polygons, this is the polygon ID (\code{poly_ID}) that groups vertices into
individual geometries. For points, this is the feature ID (\code{feat_ID}).}

\item{sdimx, sdimy}{\code{character}. Names of columns containing x and y spatial
coordinate values, respectively. Used for spatial tile planning and
geometry construction.}

\item{type}{\code{character}. Either \code{"points"} or \code{"polygons"}. What type of
geometry to write.}

\item{contiguous}{\code{logical} (default = TRUE) When \code{TRUE}, tile inclusivity
rules (see \link{getBoundedData}) prevent duplication of data at tile boundaries
(assuming no padding).}

\item{dry_run}{\code{logical} (default = FALSE). When \code{TRUE}, runs tile planning
and plots the adaptive tile layout with record counts, then returns without
writing.}

\item{write_param}{named \code{list} (optional). Additional params to pass to the
writing function.}

\item{...}{additional params to pass to \code{tileApply}}

\item{part_col}{\code{character} (optional, polygons only). Column name for
multi-part polygon grouping. See \link[GiottoClass:createGiottoPolygon]{GiottoClass::createGiottoPolygon}.}

\item{group_col}{\code{character}. If \code{type = "polygons"}, name of column used
for spatial tile planning via envelope centroids. Defaults to \code{id_col}.}
}
\description{
Write geometry data to hive-partitoned spatially tiled parquets. The partition
column is \code{"tile_index"}.
\subsection{Accepted \code{data} inputs}{
\itemize{
\item \code{queryableStore}-inheriting - Any dplyr lazy queryable store. Requires
selection of \code{type} param.
\itemize{
\item \code{"points"} - written directly.
\item \code{"polygons"} - must already be written to \code{"parquetStore"}-inheriting
class. Computes geometries from vertices which requires row ordering.
}
\item \code{parquetGeomStore}-inheriting - Simple write to a new tiled store.
}
}
}
\seealso{
Other storeWrite methods: 
\code{\link{storeWrite}()},
\code{\link{storeWrite-parquetGeomStore}},
\code{\link{storeWrite-parquetStore}}
}
\concept{storeWrite methods}
