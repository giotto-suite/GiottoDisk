% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream-markers.R
\name{analyzeData-scranMarkersParam}
\alias{analyzeData-scranMarkersParam}
\title{Streaming pairwise marker detection}
\usage{
\S4method{analyzeData}{parquetExprBase,scranMarkersParam}(x, param, ..., groups = NULL)
}
\arguments{
\item{x}{a \code{parquetExprBase} store.}

\item{param}{a \link[Giotto:scranMarkersParam-class]{Giotto::scranMarkersParam}.}

\item{...}{additional arguments (none used).}

\item{groups}{vector of cluster assignments, one per cell of the store's
current view. \code{NA} excludes a cell from every group.}
}
\value{
A \code{SimpleList} of \code{DataFrame}s, one per group, as
\code{\link[scran]{findMarkers}} returns.
}
\description{
\code{\link[GiottoClass:analyzeData]{GiottoClass::analyzeData()}} method for a \link[Giotto:scranMarkersParam-class]{Giotto::scranMarkersParam}
on a disk-backed expression store.

The per-group statistic pass is a grouped aggregate pushed into Acero, so
the store is scanned once and the pairwise comparisons that follow never
touch it. Results are verified elementwise against
\code{\link[scran]{findMarkers}}.

\code{test_type} must be \code{"t"}: it is the only test that reduces to per-group
moments. \code{"wilcox"} needs the per-cell values to rank, and \code{"binom"} is not
wired up. Materialize with \code{storeRead(x, output = "dgcmatrix")} for those.
}
\seealso{
\code{\link[Giotto:markersParam]{Giotto::markersParam()}}, \code{\link[Giotto:findScranMarkers]{Giotto::findScranMarkers()}}
}
