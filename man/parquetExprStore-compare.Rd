% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-parquetExprStore.R
\name{parquetExprStore-compare}
\alias{parquetExprStore-compare}
\alias{Compare,parquetExprBase,numeric-method}
\alias{Compare,numeric,parquetExprBase-method}
\alias{Compare,parquetExprBase,parquetExprBase-method}
\title{Compare an expression store against a scalar}
\usage{
\S4method{Compare}{parquetExprBase,numeric}(e1, e2)

\S4method{Compare}{numeric,parquetExprBase}(e1, e2)

\S4method{Compare}{parquetExprBase,parquetExprBase}(e1, e2)
}
\arguments{
\item{e1, e2}{a \code{parquetExprBase}-inheriting store and a numeric scalar, in
either order}
}
\value{
the store, with a \code{compare} record appended to its op chain
}
\description{
\code{>}, \code{>=}, \code{<}, \code{<=}, \code{==} and \code{!=} against a numeric scalar return the
store with a lazy indicator queued on its op chain: stored entries that
pass keep value 1, entries that fail are dropped. Nothing is read until
the result is, so \code{rowSums(x >= 1)} counts per feature in one streaming
pass, and the comparison sees the values the chain produces -- after
normalization, if normalization is queued.

The result is sparse only when an unstored zero fails the comparison, so a
comparison that is \code{TRUE} at 0 (\code{x >= 0}, \code{x < 1}, \code{x == 0}, ...) is an
error rather than a silent densification. Materialize a bounded slice with
\code{storeRead(x[i, j], output = "dgcmatrix")} for those.

Passing entries read back as the double \code{1}, not \code{TRUE}, because a stored
value is a double on every read path. Sums and counts are unaffected.
}
\examples{
\dontrun{
rowSums(x >= 1)   # cells expressing each feature
colSums(x > 0)    # features detected per cell
}
}
