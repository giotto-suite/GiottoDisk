% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-fileInputs.R
\name{tenxZarrInput-class}
\alias{tenxZarrInput-class}
\title{10x Xenium/Atera Zarr Expression Input}
\description{
Wraps the \code{cell_feature_matrix.zarr.zip} archive (or unzipped \code{.zarr}
tree) shipped by 10x Xenium and Atera. Construction is metadata-only:
the feature catalog (\code{cell_features/.zattrs}), the encoded cell
barcodes, and the CSC-by-FEATURE \code{indptr} are read eagerly; the
\code{indices} / \code{data} arrays are streamed by \code{\link[=storeRead]{storeRead()}}.

Because the on-disk matrix is feature-major and the iterator contract
is cell-ordered, \code{\link[=storeRead]{storeRead()}} reorders on the fly: a two-pass
cell-major placement when the triplet buffers fit the RAM budget
(\code{mode = "full"}), or bounded per-cell-block rescans when they do not
(\code{mode = "cellblock"}; forced when nnz exceeds int32).
}
\section{Slots}{

\describe{
\item{\code{feat_types}}{character. Feature classes as 10x display strings
("Gene Expression", "Negative Control Probe", ...), aligned 1:1
with \code{feat_ids}.}

\item{\code{mode}}{character. "auto" (default), "full", or "cellblock".}

\item{\code{cells_per_block}}{integer. Cells per window in cellblock mode
(0 = derive from the RAM budget at read time).}

\item{\code{nnz}}{numeric. Stored values across kept features (double -- may
exceed int32 at Atera scale).}
}}

\seealso{
Other store types:
\code{\link{binGefInput-class}},
\code{\link{cellbinGefInput-class}},
\code{\link{csvWideInput-class}},
\code{\link{dataStore-class}},
\code{\link{edgeInput-class}},
\code{\link{exprInput-class}},
\code{\link{fileStore-class}},
\code{\link{mtxInput-class}},
\code{\link{parquetEdgeStore-class}},
\code{\link{parquetExprStore-class}},
\code{\link{parquetStore-class}},
\code{\link{tenxH5Input-class}},
\code{\link{unionParquetExprStore-class}}
}
\concept{store types}
