% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chunk-sizing.R
\name{storeChunkInfo}
\alias{storeChunkInfo}
\title{Report how a store's streaming windows are chosen}
\usage{
\S4method{storeChunkInfo}{parquetExprBase}(
  x,
  ram_frac = c(0.1, 0.15, 0.2, 0.25, 0.35, 0.5),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{x}{a \code{parquetExprStore} or \code{unionParquetExprStore}.}

\item{ram_frac}{numeric. Fractions to tabulate. Defaults to a spread around
the configured value.}

\item{verbose}{logical. Print the report (default \code{TRUE}).}

\item{...}{unused.}
}
\value{
A \code{data.table} of one row per (pass, ram_frac), invisibly.
}
\description{
Streaming passes read a store in windows sized to stay within a fraction of
free RAM. The window is \strong{derived per read} rather than stored, from the
store's shape and its cached \verb{@stats} marginals, so it adapts to the machine
doing the reading. This reports what those windows come out to and why.

Two read shapes are shown because the package has two, and they differ by
roughly 4x in memory per stored value:

\describe{
\item{\verb{sparse matrix}}{chunks land in a \code{dgCMatrix} (4-byte index +
8-byte value). Used by the PCA band loops and the \code{storeWrite()} bake.}
\item{\verb{triplet frame}}{chunks are collected as \code{row_id} / \code{col_id} /
\code{value} / \code{source_id} plus arrow and data.table overhead, measured at
47-54 bytes per stored value. Used by R-side statistic accumulation
when the op chain cannot be lowered to Acero.}
}
}
\section{Steering the window}{

\describe{
\item{\code{giottodisk.chunk_ram_frac}}{fraction of free RAM to budget per
chunk (default 0.25). Lower it on a busy machine; raise it on a
dedicated one. Scales every pass without knowing any store's shape.}
\item{\code{giottodisk.chunk_size}}{pins an absolute window in rows,
overriding the derivation entirely. Also the value used when the
derivation cannot run.}
}
}

\examples{
\dontrun{
storeChunkInfo(pe)
options(giottodisk.chunk_ram_frac = 0.10)   # halve every streaming window
}
}
