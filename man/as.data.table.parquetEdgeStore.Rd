% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/class-parquetEdgeStore.R
\name{as.data.table.parquetEdgeStore}
\alias{as.data.table.parquetEdgeStore}
\title{Coerce a parquetEdgeStore to a data.table}
\usage{
\method{as.data.table}{parquetEdgeStore}(x, ...)
}
\arguments{
\item{x}{\code{parquetEdgeStore}}

\item{\dots}{passed to \code{\link[=storeRead]{storeRead()}}}
}
\value{
data.table with \code{from}, \code{to}, and any edge attributes stored
alongside them (e.g. \code{weight}, \code{distance})
}
\description{
Read a backed network into an in-memory edge table with
character node IDs. Equivalent to \code{storeRead(x, output = "tibble")} with the
endpoint columns renamed to the \code{from}/\code{to} the rest of the suite expects.
Registered so that \code{as.data.table()} on a network subobject with a backed
\verb{@network} resolves without the caller naming this package.
}
\seealso{
\code{\link[=storeRead]{storeRead()}}
}
