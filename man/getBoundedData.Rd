% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tilework.R
\name{getBoundedData}
\alias{getBoundedData}
\alias{getBoundedData,parquetGeomStore,SpatExtent-method}
\title{Get Bounded Data}
\usage{
\S4method{getBoundedData}{queryableStore,SpatExtent}(
  x,
  bound,
  sdimx = "x",
  sdimy = "y",
  inclusive = TRUE,
  group_col = "id",
  envelope = FALSE,
  output = "query",
  ...
)

\S4method{getBoundedData}{parquetGeomStore,SpatExtent}(x, bound, inclusive = TRUE, ...)
}
\arguments{
\item{x}{\code{queryableStore} inheriting object}

\item{bound}{\code{SpatExtent}}

\item{sdimx}{\code{character}. Which column contains spatial x values
to filter on}

\item{sdimy}{\code{character}. Which column contains spatial y values
to filter on.}

\item{inclusive}{\code{logical} (length 1 or 4. Default = TRUE).
Whether bounds should be inclusive (inclusive is >=/<= vs >/<).
Ordering of inputs is bottom, left, top, right. If a single logical
is provided, it will be replicated and used for all 4 bounds.
Default is fully inclusive.}

\item{group_col}{\code{character}. Only used when \code{envelope = TRUE}.
Which column contains grouping IDs that are used to calculate the
spatial envelope.}

\item{envelope}{\code{logical} (default = FALSE). Whether to perform
bound filtering based on the envelope centroid instead of
individual x and y values so that grouped rows are not
separated by the filtering.

This is generally not needed for \code{parquetGeomStore} inheriting
objects since each row is its own geometry (point or polygon, etc)
and xy filtering is performed on the point or the centroid.}

\item{output}{\code{character}. Output format passed to \code{\link[=storeRead]{storeRead()}}.}

\item{...}{additional params to pass to \code{\link[=storeRead]{storeRead()}}}
}
\value{
output as determined by \code{output} param — see \code{\link[=storeRead]{storeRead()}}
}
\description{
Get the region of the data defined by a bounding \code{SpatExtent}.
\code{crop()} operations on \code{fileStore} objects return \code{dataStore},
but this function directly produces a usable interface.
}
\seealso{
Other tile methods: 
\code{\link{getTile}()}
}
\concept{tile methods}
