% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-arrow.R
\name{.arrow_add_row_index}
\alias{.arrow_add_row_index}
\title{arrow Utility: Add row index}
\usage{
.arrow_add_row_index(data, col = "row_index", offset = 0)
}
\arguments{
\item{data}{An object coercible to \link[arrow:RecordBatchReader]{arrow::RecordBatchReader}. Common inputs
include arrow queries (\code{arrow_dplyr_query}), \code{FileSystemDataset}, arrow
\code{Table}, or \code{data.frame}/\code{tibble}.}

\item{col}{\code{character}. Name of the column to create for the row index.
Default is \code{"row_index"}.}

\item{offset}{\code{integer}. Starting value for the row index. Default is \code{0},
meaning the first row will be assigned index \code{1}. Use non-zero offsets
when appending to existing indexed data or processing tiles.}
}
\value{
An arrow \link[arrow:RecordBatchReader]{arrow::RecordBatchReader} with the new index column added.
The indices continue sequentially across all batches.
}
\description{
Internal that adds a sequential row index column to arrow data processed
batch-by-batch. The index counter is maintained across batches using a
closure, ensuring continuous sequential numbering even when data is
processed in chunks.

This is particularly useful when writing large datasets to parquet where
row indices need to be assigned without loading the entire dataset into
memory.
}
\examples{
\dontrun{
# Add row_index starting from 1
ds <- arrow::open_dataset("path/to/data")
indexed_ds <- .arrow_add_row_index(ds)

# Add row_index starting from 1000 (for appending)
indexed_ds <- .arrow_add_row_index(ds, offset = 1000)

# Custom column name
indexed_ds <- .arrow_add_row_index(ds, col = "id")
}
}
\keyword{internal}
