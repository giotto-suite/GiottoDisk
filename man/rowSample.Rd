% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-ops.R
\name{rowSample}
\alias{rowSample}
\title{Subsample rows of a parquetStore}
\usage{
\S4method{rowSample}{parquetBase}(x, size, ...)
}
\arguments{
\item{x}{\code{parquetBase}-inheriting object}

\item{size}{\code{numeric(1)}. Maximum number of rows to return}

\item{...}{additional params (none implemented)}
}
\value{
\code{x} with sampling op appended to \verb{@ops}
}
\description{
Record a row subsampling operation on a store. The operation is lazy and
applied when reading via \code{\link[=storeRead]{storeRead()}}. If the store already has fewer
rows than \code{size}, the store is returned unchanged.

Sampling is performed by systematic (evenly-spaced) selection rather than
random sampling. Given a target of \code{size} rows from \code{n} total rows, every
\code{k}th row is selected where \code{k = ceiling(n / size)}. This is
deterministic and reproducible across sessions without a seed.

The actual number of rows returned is approximate since the rows to be
kept are determined based the value of the internal \code{row_index} col.
Depending on any preceding \code{\link[=subset]{subset()}} operations, the number of rows
surviving the sample filter may be slightly above or below \code{size}.
}
\examples{
ps <- parquetStore()
ps <- storeWrite(ps, mtcars)
ps |> rowSample(10)         # lazy, no disk read
ps |> rowSample(10) |> storeRead(output = "tibble")
}
\seealso{
\code{\link[=storeRead]{storeRead()}}, \code{\link[=subset]{subset()}}
}
