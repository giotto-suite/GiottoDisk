% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream-recommend.R
\name{sc_recommend_chunk}
\alias{sc_recommend_chunk}
\title{Recommend a streaming chunk size from free RAM}
\usage{
sc_recommend_chunk(
  n_cells,
  n_genes,
  density,
  n_hvg = min(2000L, as.integer(n_genes)),
  k = 50L,
  ram_frac = 0.25,
  verbose = TRUE
)
}
\arguments{
\item{n_cells}{numeric. Total cell count in the dataset.}

\item{n_genes}{numeric. Total gene count.}

\item{density}{numeric. Approximate fill fraction of the matrix.}

\item{n_hvg}{integer. Planned number of HVG (default 2000).}

\item{k}{integer. Planned number of PCA dimensions (default 50).}

\item{ram_frac}{numeric. Fraction of free RAM to budget per chunk
(default 0.25). Lower (0.15) on machines with heavy background
load; higher (0.40) on a dedicated analysis server.}

\item{verbose}{logical. Print the comparison table and recommendation
line. Default \code{TRUE}.}
}
\value{
Recommended chunk size (integer), returned invisibly.
}
\description{
Estimates the optimal \code{chunk_size} for streaming reads of a
\linkS4class{parquetExprStore} so that each pipeline pass stays within a
target fraction of currently free RAM. Call this once before opening
a dataset and pass the result to the store constructor.

Prints a small comparison table across RAM budgets and returns the
recommended value invisibly.
}
\examples{
chunk <- sc_recommend_chunk(2e6, 5000, density = 0.30,
                             n_hvg = 1000, k = 50)
}
