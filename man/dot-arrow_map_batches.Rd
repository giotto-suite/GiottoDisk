% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-arrow.R
\name{.arrow_map_batches}
\alias{.arrow_map_batches}
\title{arrow Utility: Map batches}
\usage{
.arrow_map_batches(X, FUN, ..., .schema = NULL, .lazy = TRUE)
}
\arguments{
\item{X}{An object coercible to \link[arrow:RecordBatchReader]{arrow::RecordBatchReader}. Common inputs
include arrow queries (\code{arrow_dplyr_query}), \code{FileSystemDataset}, arrow
\code{Table}, or \code{data.frame}/\code{tibble}.}

\item{FUN}{\code{function}. A function to apply. The first parameter should
accept a batch subset of the full dataset from  \code{X}.}

\item{\dots}{additional params to pass to \code{FUN}}

\item{.schema}{arrow \code{Schema} (optional). If not explicitly provided, the
schema will be inferred from the first batch.}

\item{.lazy}{\code{logical}. If \code{TRUE} (default), returns lazy reader. If \code{FALSE},
materializes all batches immediately.}
}
\value{
An arrow \link[arrow:RecordBatchReader]{arrow::RecordBatchReader} with transformed batches
}
\description{
Internal that applies a transformation function \code{FUN} to an arrow dataset
batch by batch rather than all at once, returning a new lazy
\code{RecordBatchReader} with the transformed data. This can be used to tell
arrow how to perform a data transformation that is not natively supported
in a memory-friendly way by performing it in batches. This implementation
is based on \code{arrow::map_batches()}. This may be superceded in arrow's own API
in the future. The code runs in R instead of arrow C++ code

\strong{Key behaviors:}
\itemize{
\item Lazy evaluation: Processes batches on-demand (unless \code{.lazy = FALSE})
\item Schema inference: If schema not provided, processes first batch to infer
output schema, then uses that schema for remaining batches
\item Streaming: Returns a reader that can be consumed incrementally without
loading entire dataset into memory
\item Preserves batch structure: Each batch is transformed independently
}
}
\examples{
\dontrun{
# Add row indices to batches
ds <- arrow::open_dataset("path/to/data")
.arrow_map_batches(ds, function(batch) {
    batch$row_id <- seq_len(nrow(batch))
    batch
})
}
}
\keyword{internal}
