% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-parquetExprStore.R
\name{bin_gef_to_parquetExprStore}
\alias{bin_gef_to_parquetExprStore}
\title{Stream-convert a Stereo-seq bin .gef file into a parquetExprStore}
\usage{
bin_gef_to_parquetExprStore(
  gef_path,
  bin_size,
  output_path,
  gene_column = c("geneName", "geneID"),
  batch_genes = 500L,
  overwrite = FALSE
)
}
\arguments{
\item{gef_path}{character. Path to a Stereo-seq bin .gef file.}

\item{bin_size}{character. e.g. \code{"bin1"}, \code{"bin50"}, \code{"bin100"} (must
match a \verb{geneExp/<bin_size>} group in the GEF).}

\item{output_path}{character. Destination Parquet file or directory.}

\item{gene_column}{character. \code{"geneName"} (default) or \code{"geneID"}.}

\item{batch_genes}{integer. Number of genes per streaming chunk.
Default 500.}

\item{overwrite}{logical. If \code{TRUE}, replace \code{output_path} if it exists.}
}
\value{
A list with two components:
\itemize{
\item \code{pe}         — a \linkS4class{parquetExprStore} with
\code{cell_ids = paste0("bin_", 1:n_bins)}.
\item \code{bin_coords} — a \code{data.table} of \verb{(bin_ID, x, y)} so callers can
construct spatial locations without a second read.
}
}
\description{
Reads a Stereo-seq bin GEF (\verb{*.tissue.gef}, \verb{*.gef}, \verb{*.raw.gef}) at a
chosen \code{bin_size} in \strong{gene-chunks} via \code{rhdf5::h5read} hyperslab and
writes one Parquet shard per chunk. Maintains a running
\verb{(x, y) → bin_ID} map so bin IDs are assigned online — no second pass
through the data is required.

GEF bin layout:

\if{html}{\out{<div class="sourceCode">}}\preformatted{geneExp/<bin_size>/expression : (x, y, count) — gene-major (run-length
                                 encoded by gene$count).
geneExp/<bin_size>/gene       : (geneName, geneID, count, ...).
}\if{html}{\out{</div>}}
}
\seealso{
\code{\link[=cellbin_gef_to_parquetExprStore]{cellbin_gef_to_parquetExprStore()}}
}
