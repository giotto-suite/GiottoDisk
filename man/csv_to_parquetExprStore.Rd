% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-parquetExprStore.R
\name{csv_to_parquetExprStore}
\alias{csv_to_parquetExprStore}
\title{Stream-convert a wide-format dense CSV into a parquetExprStore}
\usage{
csv_to_parquetExprStore(
  csv_path,
  output_path,
  cell_id_col = "cell_ID",
  skip_cols = NULL,
  row_filter_fun = NULL,
  batch_rows = 5000L,
  overwrite = FALSE
)
}
\arguments{
\item{csv_path}{character. Path to the wide-format CSV (gzip OK —
\code{data.table::fread} autodetects).}

\item{output_path}{character. Destination Parquet file (single
file when n_cells <= batch_rows) or directory (one shard per
batch otherwise).}

\item{cell_id_col}{character. Name of the column to use as cell
ID. Default \code{"cell_ID"}. The values become \code{pe@cell_ids} (after
any caller-side renaming).}

\item{skip_cols}{character. Other non-feature columns to drop
(e.g. \code{"fov"} for CosMx). All remaining columns become
features (\code{feat_ids}).}

\item{row_filter_fun}{function or \code{NULL}. Optional predicate
applied to each chunk's \code{data.table} BEFORE melting; should
return a logical vector (same length as the chunk's rows) or
the row indices to keep. Used by callers to apply
technology-specific filters (CosMx: \code{cell_ID != 0}, FOV
subset) without a separate pre-pass over the CSV.}

\item{batch_rows}{integer. Cells per streaming chunk. Default
50,000 — peak RAM ≈ batch_rows × n_genes × 16 bytes.}

\item{overwrite}{logical. If \code{TRUE}, replace \code{output_path} if it
exists.}
}
\value{
A \linkS4class{parquetExprStore} with \code{cell_ids} collected
(in chunk order) and \code{feat_ids} from the header.
}
\description{
Reads a wide-format CSV (one row per cell, one column per gene; the
first column or \code{cell_id_col} is the cell ID) in row-chunks via
\code{data.table::fread(skip = ..., nrows = ...)}, melts each chunk
wide → long (dropping zeros), and writes one Parquet shard per
chunk. \strong{No full dense matrix is ever materialized in memory} —
only \code{batch_rows} cells of nonzeros are held at a time.

This is the input format used by NanoString CosMx
(\verb{*_exprMat_file.csv}) and Vizgen MERSCOPE (\code{cell_by_gene.csv}).
}
